/**
 * Replace a blank value with fallback value.
 *
 * @template T Value type
 * @param {T} value Value
 * @param {T} fallbackValue Fallback value
 * @returns {T}
 */
declare function nvl<T>(value: T, fallbackValue: T): T;
/**
 * Replace a blank value with fallback value.
 *
 * @param {any} value Value
 * @param {any} fallbackValue Fallback value
 * @returns {any}
 */
declare function nvl(value: any, fallbackValue: any): any;
export = nvl;
